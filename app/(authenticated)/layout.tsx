import React from 'react';
import Link from 'next/link';
// Remove or adjust the CSS import if not needed
// import './layout.css'; // Adjust the path as necessary

const Layout = () => {
    return (
        <div>
            {/* Your layout content */}
        </div>
    );
};

export default Layout;